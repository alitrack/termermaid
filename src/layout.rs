use crate::graph::Graph;

/// Layout a graph using topological sort + bus-based edge routing.
pub fn layout_flowchart(_graph: &Graph) -> String {
    // TODO: full port of layout_canvas + place_td + draw_box + route_forward
    String::from("(flowchart layout — TODO)")
}
