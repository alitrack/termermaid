/// Sequence diagram data structures and layout (TODO).
pub struct Sequence {
    pub labels: Vec<String>,
}

pub fn parse_sequence(_src: &str) -> Option<Sequence> {
    None
}

pub fn layout_sequence(_seq: &Sequence) -> String {
    String::from("(sequence layout — TODO)")
}
